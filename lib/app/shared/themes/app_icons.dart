import 'package:bag_finder_frontend/app/shared/themes/app_dimensions.dart';
import 'package:bag_finder_frontend/app/shared/themes/app_colors.dart';
import 'package:flutter/material.dart';

class AppIconsPrimary {
  static Icon get homeIcon => Icon(
        Icons.home,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get historyIcon => Icon(
        Icons.history,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get searchIcon => Icon(
        Icons.search,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get filterIcon => Icon(
        Icons.filter_list,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get messageIcon => Icon(
        Icons.message,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get emailIcon => Icon(
        Icons.email,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get passwordIcon => Icon(
        Icons.lock,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get personIcon => Icon(
        Icons.person,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get luggageIcon => Icon(
        Icons.luggage,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get idCardIcon => Icon(
        Icons.credit_card,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get calendarIcon => Icon(
        Icons.calendar_today,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get phoneIcon => Icon(
        Icons.phone,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );
}

class AppIconsSecondary {
  static Icon get homeIcon => Icon(
        Icons.home,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get historyIcon => Icon(
        Icons.history,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get searchIcon => Icon(
        Icons.search,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get filterIcon => Icon(
        Icons.filter_list,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get messageIcon => Icon(
        Icons.message,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get emailIcon => Icon(
        Icons.email,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get passwordIcon => Icon(
        Icons.lock,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get personIcon => Icon(
        Icons.person,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get luggageIcon => Icon(
        Icons.luggage,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get idCardIcon => Icon(
        Icons.credit_card,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get calendarIcon => Icon(
        Icons.calendar_today,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get phoneIcon => Icon(
        Icons.phone,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );
}

class AppIconsSecondaryGrey {
  static Icon get homeIcon => Icon(
        Icons.home,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get historyIcon => Icon(
        Icons.history,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get searchIcon => Icon(
        Icons.search,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get filterIcon => Icon(
        Icons.filter_list,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get messageIcon => Icon(
        Icons.message,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get emailIcon => Icon(
        Icons.email,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get passwordIcon => Icon(
        Icons.lock,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get personIcon => Icon(
        Icons.person,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get luggageIcon => Icon(
        Icons.luggage,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get idCardIcon => Icon(
        Icons.credit_card,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get calendarIcon => Icon(
        Icons.calendar_today,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get phoneIcon => Icon(
        Icons.phone,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );
}
