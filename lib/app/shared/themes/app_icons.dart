import 'package:bag_finder_frontend/app/shared/themes/app_dimensions.dart';
import 'package:flutter/material.dart';

class AppIcons {
  static Icon get homeIcon =>
      const Icon(Icons.home, size: AppDimensions.iconMedium);

  static Icon get emailIcon =>
      const Icon(Icons.email, size: AppDimensions.iconMedium);

  static Icon get passwordIcon =>
      const Icon(Icons.lock, size: AppDimensions.iconMedium);

  static Icon get personIcon =>
      const Icon(Icons.person, size: AppDimensions.iconMedium);

  static Icon get luggageIcon =>
      const Icon(Icons.luggage, size: AppDimensions.iconMedium);

  static Icon get idCardIcon =>
      const Icon(Icons.credit_card, size: AppDimensions.iconMedium);

  static Icon get calendarIcon =>
      const Icon(Icons.calendar_today, size: AppDimensions.iconMedium);

  static Icon get phoneIcon =>
      const Icon(Icons.phone, size: AppDimensions.iconMedium);
}
